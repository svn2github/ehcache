<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>net.sf.ehcache</groupId>
        <artifactId>ehcache-parent</artifactId>
        <version>1.3.0-SNAPSHOT</version>
    </parent>
    <name>Ehcache Debugger</name>
    <artifactId>ehcache-debugger</artifactId>
    <packaging>jar</packaging>
    <description>Ehcache bundled with RemoteDebugger</description>
    <dependencies>
        <dependency>
            <groupId>${pom.groupId}</groupId>
            <artifactId>ehcache</artifactId>
            <version>${pom.version}</version>
        </dependency>
        <dependency>
            <groupId>commons-logging</groupId>
            <artifactId>commons-logging</artifactId>
            <scope>compile</scope>
        </dependency>        
    </dependencies>
    <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>shade-maven-plugin</artifactId>
            <version>1.0-alpha-4</version>
            <executions>
              <execution>
                <phase>package</phase>
                <goals>
                  <goal>shade</goal>
                </goals>
                <configuration>
                  <!-- Add any artifact that should not be included in the uber jar -->
                  <artifactSet>
                    <excludes>
                    </excludes>
                  </artifactSet>
                  <!-- Note: requires at least one relocation config - even if dummy - it fails otherwise -->
                  <relocations>
                    <relocation>
                      <pattern>package.to.relocate</pattern>
                    </relocation>
                  </relocations>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
    </build>
</project>
