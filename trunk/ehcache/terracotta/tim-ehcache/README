README
------

1. Clean 

   $ mvn clean
   $ mvn -Psystem-tests clean (also clean test folders)

2. Compile

   $ mvn install

3. Run Tests

   $ mvn -Psystem-tests integration-test (all tests)
   $ mvn -Psystem-tests integration-test -Dtest=Testname (1 specific test)

   eg:
   $ mvn -Psystem-tests integration-test -Dtest=CacheElementEviction130Test
