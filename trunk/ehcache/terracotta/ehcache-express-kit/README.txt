
To create a Ehcache Terracotta distribution kit, you will need to have a Terracotta kit (.tar.gz)

Syntax:

mvn package -P package-tc -Dtc-kit-url=http://..../terracotta-xxx.tar.gz

or

mvn package -P package-tc -Dtc-kit-url=/path/to/terracotta-xxx.tar.gz

