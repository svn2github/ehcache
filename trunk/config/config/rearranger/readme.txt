This directory contains a config.xml file for rearranger, a formatting tool which will order the parts of a Java file 
as required by the Sun standards and enforced by checkstyle rules.

In IntelliJ, Go to Plugins -> Rearranger -> Configuration and import it.
